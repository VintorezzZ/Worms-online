using DefaultNamespace;
using Unity.Netcode;
using UnityEngine;

namespace Server
{
    public class ServerWeapon : BaseWeapon
    {
        protected override void Attack()
        {
            base.Attack();
        }
        
    }
}